{
  "name": "elliptic-curve-basic",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc --build",
    "start": "rimraf dist && tsc --build && node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
